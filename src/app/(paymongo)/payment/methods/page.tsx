import PaymentMethods from '@/components/payment/PaymentMethods'
import { FC } from 'react'

interface Props {
  
}

const page: FC<Props> = ({}) => {
  return(
    <PaymentMethods/>
  )
}

export default page
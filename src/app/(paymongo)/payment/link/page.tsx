import PaymentLink from '@/components/payment/PaymentLink'
import { FC } from 'react'

interface Props {
  
}

const page: FC<Props> = ({}) => {
  return(
    <PaymentLink/>
  )
}

export default page